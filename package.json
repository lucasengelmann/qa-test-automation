{
  "name": "projeto-qa-estudos",
  "version": "1.0.0",
  "description": "Projeto QA completo: Playwright + Cucumber + K6 + Newman + CI/CD",
  "scripts": {
    "test:e2e": "cucumber-js --profile default",
    "test:e2e:headed": "HEADLESS=false cucumber-js --profile default",
    "test:smoke": "cucumber-js --profile smoke",
    "test:regression": "cucumber-js --profile regression",
    "test:api": "newman run postman/collections/restful-booker.json -e postman/environments/local.json --reporters cli,htmlextra --reporter-htmlextra-export reports/newman-report.html",
    "test:performance": "k6 run performance/scenarios/load.js",
    "test:performance:smoke": "k6 run performance/scenarios/smoke.js",
    "test:all": "npm run test:api && npm run test:e2e && npm run test:performance",
    "report:open": "open reports/cucumber-report.html || xdg-open reports/cucumber-report.html"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.0.0",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}